{% block block_center_button %}
    {% set button = block.slots.getSlot('button') %}

    <div class="center-button">
        <a class="btn btn-primary btn-get-started" href="{{ button.config.buttonLink.value | raw }}">{{ button.config.buttonText.value | raw }}</a>
    </div>
{% endblock %}
